import Travel from "./travel"


function App() {
  
  return (
    /* <> this symbol is called fragement, the rule of the jsx is that we only return one html element so if you want to return multiple elements we wrapped those elemets to this fragement.*/
    <> 
    <Travel/>
    <h1> Hello explorar </h1>
    
    </>
    
  )
}

export default App
